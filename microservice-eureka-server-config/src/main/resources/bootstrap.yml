spring:
  application:
    name: microservice-eureka-server-config
  cloud:
    config:
      name: eureka_config
      uri: http://config-zzd.com:4001  # 配置configserver地址
      profile: dev  # 级别
      label: main  # 分支 git中 默认main

